version: "3.9"

services:

  api:
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512MB
    image: kanap
    container_name: kanap-api
    build: .
    ports:
      - 3000:3000
    volumes:
      - ./back:/app/back
    command: sh -c "cd back && npm i && node server"

  web:
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512MB
    image: kanap
    container_name: kanap-web
    build: .
    ports:
      - 8080:8080
    volumes:
      - ./back:/app/back
    command: sh -c "cd back && live-server --port=8080 --no-browser --host=0.0.0.0"
